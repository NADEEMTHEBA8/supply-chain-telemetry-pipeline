# Airflow pipeline dependencies — baked into the custom Docker image.
# This file is consumed by infra/airflow/Dockerfile.airflow.
#
# Keep versions in sync with host-side pyproject.toml to maintain
# 12-Factor X (Dev/Prod Parity) between local host processes and the
# Airflow container.

# dbt — Postgres adapter pinned to match the warehouse/dbt/fraud_warehouse
# dbt_project.yml requirements. Pinned to 1.8.x series.
dbt-core==1.8.7
dbt-postgres==1.8.2

# Redis client — used by feature_store.src.loader (called from Airflow DAG)
redis>=5.0.0,<6.0.0

# Postgres client — used by feature_store.src.loader
psycopg2-binary>=2.9.0,<3.0.0

# Config — used by config.settings (imported by all pipeline modules)
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Logging — used by config.logging_config (imported by loader.py)
python-json-logger>=2.0.7,<3.0.0
