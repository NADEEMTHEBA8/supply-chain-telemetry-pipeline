"""
config — Centralised application configuration package.

Import the singleton `settings` object from this package:

    from config.settings import settings
"""

from config.settings import settings

__all__ = ["settings"]
